---
/**
 * Header Component
 * Main navigation header with responsive variants
 * 
 * Variants:
 * - 'mobile': Mobile header with brand, search button, menu button, and close button
 * - 'mobile-search': Mobile search mode with back button and full-width search bar
 * - 'desktop': Desktop header with brand and search bar (centered, max-width 968px)
 * - 'desktop-home': Desktop home header with brand and full-width search bar (max-width 1260px)
 * - 'responsive': Auto-switches between mobile (<769px) and desktop (>=769px)
 * - 'responsive-home': Auto-switches between mobile (<769px) and desktop-home (>=769px)
 * 
 * Props:
 * @param variant - Header layout variant (default: 'responsive')
 * @param homeUrl - URL for brand link (default: '/')
 * @param searchDisabled - Whether search bar is disabled (default: false)
 * @param searchButtonDisabled - Whether search button is disabled in mobile variant (default: false)
 * @param showCloseButton - Whether to show close button in mobile variant (default: false)
 * 
 * Usage:
 * <Header variant="responsive" homeUrl="/" /> <!-- Auto mobile/desktop switching -->
 * <Header variant="mobile" homeUrl="/" searchDisabled={false} searchButtonDisabled={false} showCloseButton={false} />
 * <Header variant="mobile-search" homeUrl="/" searchDisabled={false} />
 * <Header variant="desktop" homeUrl="/" searchDisabled={false} />
 * <Header variant="desktop-home" homeUrl="/" searchDisabled={false} />
 * <Header variant="responsive-home" homeUrl="/" /> <!-- Auto mobile/desktop-home switching for home page -->
 * 
 * Design System Notes:
 * - Background: --surface-light-mid (neutral white strong)
 * - Border: --on-surface-light-border-low bottom border
 * - Brand text: --on-surface-light-regular
 * - Spacing: 16px padding, 8px/12px gaps
 * - Typography: Public Sans for brand, Inter for search input
 * - Breakpoint: 769px (--breakpoint-tablet-md)
 */

import ButtonSearch from './ButtonSearch.astro';
import ButtonMenu from './ButtonMenu.astro';
import ButtonClose from './ButtonClose.astro';
import ButtonBack from './ButtonBack.astro';
import InputSearchBar from './InputSearchBar.astro';
import IconRocketColorGradient from '../icons/brand/IconRocketColorGradient.astro';

export interface Props {
  variant?: 'mobile' | 'mobile-search' | 'desktop' | 'desktop-home' | 'responsive' | 'responsive-home';
  homeUrl?: string;
  searchDisabled?: boolean;
  searchButtonDisabled?: boolean;
  showCloseButton?: boolean;
  /** Show search toggle buttons (popover + checkbox fallback) */
  showSearchToggle?: boolean;
  /** Show search unavailable toggle + dialog */
  showSearchUnavailable?: boolean;
  /** Checkbox ID for no-JS search toggle fallback */
  searchToggleCheckboxId?: string;
  /** Checkbox ID for no-JS search unavailable fallback */
  searchUnavailableCheckboxId?: string;
}

const { 
  variant = 'responsive',
  homeUrl = '/',
  searchDisabled = false,
  searchButtonDisabled = false,
  showCloseButton = false,
  showSearchToggle = false,
  showSearchUnavailable = false,
  searchToggleCheckboxId = 'search_toggle_input',
  searchUnavailableCheckboxId = 'search_unavailable_input',
} = Astro.props;

// For responsive variants, render both mobile and desktop
const showMobile = variant === 'mobile' || variant === 'responsive' || variant === 'responsive-home';
const showDesktop = variant === 'desktop' || variant === 'responsive';
const showDesktopHome = variant === 'desktop-home' || variant === 'responsive-home';
---

<header class="header" data-variant={variant}>
  <!-- Border Overlay -->
  <div class="border-overlay" aria-hidden="true"></div>
  
  {showMobile && (
    <!-- Mobile Variant: Brand + Action Buttons -->
    <div class="header-content mobile-content">
      <!-- Brand -->
      <a href={homeUrl} class="brand brand-link">
        <div class="brand-icon">
          <IconRocketColorGradient />
        </div>
        <div class="brand-text">
          <span class="brand-text-bold">FF</span>
          <span class="brand-text-regular">Apps Archive</span>
        </div>
      </a>
      
      
      
      <!-- Action Buttons -->
      <div class="action-buttons">
        {!showSearchToggle && !showSearchUnavailable && <ButtonSearch disabled={searchButtonDisabled} />}
        <ButtonMenu popovertarget="nav" />
        {showCloseButton && <ButtonClose />}
      </div>
    </div>
  )}
  
  {variant === 'mobile-search' && (
    <!-- Mobile Search Variant: Back Button + Search Bar -->
    <div class="header-content mobile-search-content">
      <ButtonBack />
      <div class="search-bar-wrapper">
        <!-- Search Form with Popover + Toggle Buttons -->
        <InputSearchBar 
          asForm
          popover
          showToggle={showSearchToggle}
          showUnavailable={showSearchUnavailable}
          toggleCheckboxId={searchToggleCheckboxId}
          unavailableCheckboxId={searchUnavailableCheckboxId}
          disabled={searchDisabled}
        />
      </div>
    </div>
  )}
  
  {showDesktop && (
    <!-- Desktop Variant: Centered with Brand + Search Bar -->
    <div class="header-content-wrapper desktop-wrapper">
      <div class="header-content desktop-content">
        <!-- Brand -->
        <a href={homeUrl} class="brand desktop-brand brand-link">
          <div class="brand-icon">
            <IconRocketColorGradient />
          </div>
          <div class="brand-text desktop-brand-text">
            <span class="brand-text-bold desktop-text-bold">FF</span>
            <span class="brand-text-regular desktop-text-regular">Apps Archive</span>
          </div>
        </a>
        
        <!-- Search Bar -->
        <div class="search-bar-container">
          <InputSearchBar asForm disabled={searchDisabled} />
        </div>
      </div>
    </div>
  )}
  
  {showDesktopHome && (
    <!-- Desktop Home Variant: Brand + Full Search Bar -->
    <div class="header-content desktop-home-content">
      <!-- Brand -->
      <a href={homeUrl} class="brand desktop-brand brand-link">
        <div class="brand-icon">
          <IconRocketColorGradient />
        </div>
        <div class="brand-text desktop-brand-text">
          <span class="brand-text-bold desktop-text-bold">FF</span>
          <span class="brand-text-regular desktop-text-regular">Apps Archive</span>
        </div>
      </a>
      
      <!-- Search Bar -->
      <div class="search-bar-container">
        <InputSearchBar asForm disabled={searchDisabled} />
      </div>
    </div>
  )}
</header>

<style>
  /* ========================================
     HEADER BASE
     ======================================== */
  .header {
    position: relative;
    width: 100%;
    height: auto;
    background: var(--surface-light-low);
    padding: var(--spacing-16);
    transition: all 0.2s ease;
  }

  /* Border overlay (bottom border) */
  .border-overlay {
    position: absolute;
    inset: 0;
    border-bottom: var(--border-sm) solid var(--on-surface-light-border-low);
    pointer-events: none;
  }

  /* ========================================
     MOBILE VARIANT
     ======================================== */
  .mobile-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    max-width: 968px;
    margin: 0 auto;
  }

  /* Brand Section */
  .brand {
    display: flex;
    align-items: center;
    gap: var(--spacing-8);
  }

  /* Brand Link Styles */
  .brand-link {
    text-decoration: none;
    cursor: pointer;
    border-radius: var(--radius-md);
    transition: all 0.2s ease;
  }

  /* Hover state - overridden by focus */
  .brand-link:hover:not(:focus-visible) {
    opacity: 0.8;
  }

  /* Focus state - takes precedence over hover */
  .brand-link:focus-visible {
    outline: var(--border-lg) solid var(--on-surface-light-faded);
    outline-offset: 2px;
    opacity: 0.8;
  }

  /* Active state - click feedback */
  .brand-link:active {
    transform: scale(0.98);
  }

  .brand-icon {
    width: 32px;
    height: 32px;
    flex-shrink: 0;
  }

  .brand-icon :global(svg) {
    width: 100%;
    height: 100%;
  }

  .brand-text {
    display: flex;
    align-items: flex-end;
    gap: var(--spacing-4);
    color: var(--on-surface-light-regular);
    white-space: nowrap;
    line-height: 0;
  }

  .brand-text-bold {
    font-family: 'Public Sans', sans-serif;
    font-weight: 700;
    font-size: 18px;
    line-height: 20px;
    letter-spacing: 0;
  }

  .brand-text-regular {
    font-family: 'Public Sans', sans-serif;
    font-weight: 400;
    font-size: 16px;
    line-height: 20px;
    letter-spacing: 0;
  }

  /* Action Buttons */
  .action-buttons {
    display: flex;
    align-items: center;
    gap: var(--spacing-12);
    height: 32px;
  }

  /* ========================================
     MOBILE SEARCH VARIANT
     ======================================== */
  .mobile-search-content {
    display: flex;
    align-items: center;
    gap: var(--spacing-12);
    width: 100%;
  }

  .search-bar-wrapper {
    flex: 1 0 0;
    min-width: 0;
  }

  /* ========================================
     DESKTOP VARIANT
     ======================================== */
  .desktop-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
  }

  .desktop-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    max-width: 1260px;
    min-width: 260px;
  }

  .desktop-brand {
    display: flex;
    align-items: center;
    gap: var(--spacing-8);
  }

  .desktop-brand-text {
    display: flex;
    align-items: flex-end;
    gap: var(--spacing-4);
    color: var(--on-surface-light-regular);
    white-space: nowrap;
    line-height: 0;
  }

  .desktop-text-bold {
    font-family: 'Public Sans', sans-serif;
    font-weight: 700;
    font-size: 24px;
    line-height: 20px;
    letter-spacing: 0;
  }

  .desktop-text-regular {
    font-family: 'Public Sans', sans-serif;
    font-weight: 400;
    font-size: 20px;
    line-height: 20px;
    letter-spacing: 0;
  }

  .search-bar-container {
    width: 287px;
    height: 32px;
  }

  /* ========================================
     DESKTOP HOME VARIANT
     ======================================== */
  .desktop-home-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    max-width: 968px;
    margin: 0 auto;
  }

  /* ========================================
     RESPONSIVE VARIANT
     Breakpoint: 769px (--breakpoint-tablet-md)
     ======================================== */
  
  /* When responsive: hide desktop on mobile, hide mobile on desktop */
  .header[data-variant="responsive"] .mobile-content {
    display: flex;
  }
  
  .header[data-variant="responsive"] .desktop-wrapper {
    display: none;
  }
  
  @media (min-width: 769px) {
    .header[data-variant="responsive"] .mobile-content {
      display: none;
    }
    
    .header[data-variant="responsive"] .desktop-wrapper {
      display: flex;
    }
  }

  /* ========================================
     RESPONSIVE-HOME VARIANT
     Switches between mobile and desktop-home
     ======================================== */
  .header[data-variant="responsive-home"] .mobile-content {
    display: flex;
  }
  
  .header[data-variant="responsive-home"] .desktop-home-content {
    display: none;
  }
  
  @media (min-width: 769px) {
    .header[data-variant="responsive-home"] .mobile-content {
      display: none;
    }
    
    .header[data-variant="responsive-home"] .desktop-home-content {
      display: flex;
    }
  }
</style>